// Check if every number in this array is grater than zero. 

let numbers = [1, 3, 5, 10, 43, 64, 8];

console.log(numbers.every(element => element > 0))

// Here we are using the .every() Method which takes in a 'test' condition and will test every element to see if it pass it. It will return a True or False.